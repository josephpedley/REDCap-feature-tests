name: Cypress Manual Run

on:
  workflow_dispatch:

jobs:
  run-cypress:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        folder:
          - "Data Access Groups"
          - "Data Collection And Storage"
          - "Data Entry"
          - "Data Import"
          - "Data Integrity"
          - "Data Quality"
          - "Logging Module"
          - "Online Designer"
          - "Project-Level Management"
          - "Reporting"
          - "User Access Control"

    env:
      ADMIN_USERNAME: ${{ secrets.ADMIN_USERNAME }}
      ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
      TESTUSER_USERNAME: ${{ secrets.TESTUSER_USERNAME }}
      TESTUSER_PASSWORD: ${{ secrets.TESTUSER_PASSWORD }}
      BASE_URL: ${{ secrets.BASE_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress
        run: |
          echo "Running tests for folder: ${{ matrix.folder }}"
          mkdir -p cypress-results cypress/reports
          npx cypress run \
            --env BASE_URL=${BASE_URL},ADMIN_USERNAME=${ADMIN_USERNAME},ADMIN_PASSWORD=${ADMIN_PASSWORD},TESTUSER_USERNAME=${TESTUSER_USERNAME},TESTUSER_PASSWORD=${TESTUSER_PASSWORD} \
            --spec "cypress/e2e/${{ matrix.folder }}/**/*.cy.js" \
            --reporter mochawesome \
            --reporter-options reportDir=cypress/reports,overwrite=false,html=false,json=true,reportFilename=report-${{ matrix.folder | replace(' ', '_') }} \
            > cypress-results/test-report.log 2>&1 || true

      - name: Upload JSON report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "${{ matrix.folder }} - JSON"
          path: cypress/reports/*.json

      - name: Upload log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "${{ matrix.folder }} - Console Log"
          path: cypress-results/test-report.log

  merge-reports:
    needs: run-cypress
    runs-on: ubuntu-latest

    steps:
      - name: Download all JSON artifacts
        uses: actions/download-artifact@v4
        with:
          path: combined-reports

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install mochawesome tools
        run: npm install mochawesome mochawesome-merge marge --no-save

      - name: Merge JSON reports
        run: |
          mkdir -p merged
          find combined-reports -name "*.json" -exec cp {} merged/ \;
          npx mochawesome-merge merged/*.json > merged/merged-report.json

      - name: Generate full HTML report
        run: |
          npx marge merged/merged-report.json -f full-report -o merged --inline

      - name: Upload final combined report
        uses: actions/upload-artifact@v4
        with:
          name: Combined Cypress HTML Report
          path: merged/full-report.html
